# [TICKET]

**ID:** TICKET-20260528-001  
**Utente:** Princhi  
**Urgenza:** 🟡 Media  
**Status:** OPEN  
**Data Creazione:** 2026-05-28 09:00:00  
**Creato da:** BOT  

---

## 📋 Descrizione Problema

Il campo `featured` non esiste nella tabella `products` del database.
Il bot non riusciva a verificare la struttura del database per implementare
la feature "Prodotti in Evidenza" (TASK 2).

---

## 📁 File Coinvolti

- /components/models/Product.php
- /components/config/db.php

---

## 🔍 Root Cause Ipotizzata

Database `my_cybers` inizializzato senza uno schema SQL esterno.
Nessun file `.sql` trovato per creare le tabelle base.
Il bot non ha accesso diretto al database per eseguire query di verifica.

---

## ✅ Cosa Serve per Risolvere

- Fornire file `schema.sql` con CREATE TABLE complete per tutte le tabelle
- Oppure eseguire manualmente: `ALTER TABLE products ADD COLUMN featured TINYINT(1) DEFAULT 0;`
- Oppure fornire credenziali DB per eseguire query direttamente

---

## 📝 Note

Questo ticket è un esempio. Per il sistema completo di ticket automatici,
consultare `/BOT_INSTRUCTIONS.md`.

**TICKET STATO:** Risolto manualmente aggiungendo SQL migration in `/sql_migrations/add_featured_to_products.sql`
